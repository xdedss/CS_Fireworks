using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using UnityEngine;

namespace CS_Fireworks
{
    class FireworksTestClass : MonoBehaviour
    {
        private void Start()
        {
            
        }

        private void Update()
        {
            
        }

        private void OnGUI()
        {
            GUI.Label(new Rect(0, 0, 500, 100), "testclass exists");
        }
    }
}
